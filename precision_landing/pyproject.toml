[project]
name = "precision-landing"
version = "1.0.0"
description = "Drone precision landing system using ArUco/AprilTag detection for Raspberry Pi 5"
requires-python = ">=3.10"

[tool.pyre]
search_path = ["."]
source_directories = ["."]
